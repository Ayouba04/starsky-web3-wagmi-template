# Faster npm installs
prefer-offline=true
audit=false
fund=false
progress=false
loglevel=warn

# Use npm registry with better caching
registry=https://registry.npmjs.org/

# Reduce package-lock noise
package-lock=true
save-exact=false
